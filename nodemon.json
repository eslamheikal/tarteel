{
  "watch": [
    "local-server.ts",
    "api/**/*.ts"
  ],
  "ext": "ts,js,json",
  "ignore": [
    "node_modules/**/*",
    "dist/**/*",
    "src/**/*"
  ],
  "exec": "ts-node --transpile-only local-server.ts",
  "env": {
    "NODE_ENV": "development"
  }
}
